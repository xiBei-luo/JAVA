<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.luo.dao.ExcelDao">
    <sql id="BASE_TABLE">
        banktb
    </sql>

    <sql id="BASE_COLUMN">
        id,docNumber,account,firstTime,flag,unit,remark,info,moneyOut,moneyIn,timeEnd
    </sql>

    <insert id="addUser" parameterType="java.util.ArrayList">
        INSERT INTO
        <include refid="BASE_TABLE"/>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            docNumber,account,firstTime,flag,unit,remark,info,moneyOut,moneyIn,timeEnd
        </trim>
        <trim prefix="VALUES(" suffix=")" suffixOverrides=",">
            #{docNumber, jdbcType=VARCHAR},#{account, jdbcType=VARCHAR},#{firstTime, jdbcType=VARCHAR},#{flag, jdbcType=VARCHAR},#{unit, jdbcType=VARCHAR},#{remark, jdbcType=VARCHAR},#{info, jdbcType=VARCHAR},#{moneOut, jdbcType=VARCHAR},#{moneyIn, jdbcType=VARCHAR},#{timeEnd, jdbcType=VARCHAR}
        </trim>
    </insert>

    <insert id="addUser" parameterType="java.util.ArrayList">
        insert into
        <include refid="BASE_TABLE"/>
        (
        id,
        rentalInfoId,
        itemName,
        number,
        remark
        )
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.id},
            #{item.rentalInfoId},
            #{item.itemName},
            #{item.number},
            #{item.remark}
            )
        </foreach>

    </insert>


</mapper>
